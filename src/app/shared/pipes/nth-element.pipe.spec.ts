import { NthElementPipe } from './nth-element.pipe';

describe('NthElementPipe', () => {
  it('create an instance', () => {
    const pipe = new NthElementPipe();
    expect(pipe).toBeTruthy();
  });
});
